// Veracode security scans (SCA + IaC/secrets on every build, SAST/Policy on
// the default branch after a merge). All logic lives in the shared library; this file only
// selects the library version. To diverge this repo, pin a different tag or
// pass overrides, for example:
//   veracodePipeline(appName: 'org/this-repo', sourceDir: 'app', topLevelBranches: 'main|release/.*')
@Library('veracode-pipeline@v1') _

veracodePipeline()
